server:
  port: 20000

spring:
  application:
    name: dream-cloud-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lowerCaseServiceId: true
    config:
#      uri: http://localhost:16000/
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: dream-cloud-config
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:11000/eureka/


